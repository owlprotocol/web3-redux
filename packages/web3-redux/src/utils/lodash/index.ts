import lodash from 'lodash';

export const {
    pick,
    cloneDeep,
    map,
    mapValues,
    groupBy,
    omit,
    isMatch,
    reduce,
    flatten,
    compact,
    uniq,
    keyBy,
    filter,
} = lodash;
