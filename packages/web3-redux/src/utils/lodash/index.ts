import lodash from 'lodash';

export const {
    pick,
    cloneDeep,
    map,
    mapValues,
    groupBy,
    omit,
    omitBy,
    isMatch,
    isUndefined,
    reduce,
    flatten,
    compact,
    uniq,
    keyBy,
    filter,
} = lodash;
