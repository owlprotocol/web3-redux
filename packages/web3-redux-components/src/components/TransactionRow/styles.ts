import styled from 'styled-components';

export const RowContainer = styled.div`
    white-space: nowrap;
    width: fit-content;
    font-size: 1rem;
    font-weight: 500;

    a {
        text-decoration: none;
        color: #5092c5;
    }
`;
