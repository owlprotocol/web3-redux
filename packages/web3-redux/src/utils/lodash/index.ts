import lodash from 'lodash';

export const {
    pick,
    cloneDeep,
    map,
    mapValues,
    groupBy,
    omit,
    omitBy,
    isEqual,
    isMatch,
    isUndefined,
    reduce,
    flatten,
    compact,
    uniq,
    keyBy,
    filter,
    zip,
} = lodash;
