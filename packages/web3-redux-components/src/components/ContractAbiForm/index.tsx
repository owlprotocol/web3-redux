import ContractSample from 'contract.sample.json'

const AbiEntityDisplay = ({ entityName, entityType }: Props) => {
    return (
        <div>
            {console.log(ContractSample)}
        </div>
    );
};

export default AbiEntityDisplay;
