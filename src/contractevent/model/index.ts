import { Interface, Id, IdDeconstructed, ReturnValues, validate, getId, getIdDeconstructed } from './interface';
import Model from './orm';

export type { Interface, Id, IdDeconstructed, ReturnValues, Model };
export { validate, getId, getIdDeconstructed };
